# Copia este archivo como .env para pruebas LOCALES y rellena los valores.
# NUNCA subas el .env real a GitHub (ya está en .gitignore).
# En el hosting, define estas mismas variables en su panel de Environment.

# (Opcional) link de 12 h para arrancar ya con datos frescos:
MB_RT_URL=

# Bot de renovación automática (si las dejas vacías, el bot se desactiva
# y la app usa MB_RT_URL o el snapshot incluido):
GMAIL_ADDRESS=geometrobus@gmail.com
GMAIL_APP_PASSWORD=
MB_RESEND_URL=https://metrobus-gtfs.sinopticoplus.com/gtfs-api/senderEmailGtfs/1339/geometrobus@gmail.com
